# Agent Rule Routing

Read only the rule files relevant to the current task.

<rules-map>

## Quick Choice Rules

* `<data-fetching-default>`
* `<screen-state-default>`
* `<form-default>`
* `<coordination-cache-default>`
