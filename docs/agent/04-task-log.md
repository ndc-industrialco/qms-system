# Task Log

- [x] Analyze current repository and build AI-optimized documentation system.
- [x] Document KPI state across the documentation system.
- [x] Implement Option B: DB Action Token for secure email links (`ActionTokenService`), integrated into KPI and DAR flows.
- [x] Fix Email Idempotency issue for KPI Recall/Re-submit (append token slice to idempotency key).
- [x] Implement KPI Approval workflow with standard signature handling (prepare sign to reviewer sign, via `ApprovalSignature`).
- [x] Standardize KPI Monthly Report Approval workflow (Added Reviewer assign dialog, signatures, tokens, and fixed 409 state transition).
- [x] Split repository workflow guidance out of `AGENTS.md` into dedicated `rules/agent-*.md` files and reduced `AGENTS.md` to an entrypoint.
- [x] Fixed Email Action Token redirect routing for KPI Monthly `REVIEWER` role.
- [x] Added reusable cross-project templates in `portable/AGENTS.template.md` and `portable/rules/agent-*.template.md` with placeholders for project-specific values.
- [x] Converted `ui-rule-example` design guidance into dedicated shared UI rules under `rules/ui-*.md` and aligned `docs/standards/ui.md`.
- [x] Implement CAR domain â€” Phase 1 (Prisma schema: 6 models, 3 enums, `CAR` added to `ApprovalModule`). DB pushed via `prisma db push`.
- [x] Implement CAR domain â€” Phase 2 (backend: `carSequenceRepository.ts`, `carRepository.ts`, `carService.ts`, `carEmailService.ts`, `lib/validations/car.ts`). Extended `AuditService` action/resource types for CAR.
- [x] Implement CAR domain â€” Phase 3 (API routes: GET/POST `/api/car`, CRUD `/api/car/[id]`, issue/respond/verify/close/re-car/next-number).
- [x] Implement CAR domain â€” Phase 4â€“5 (frontend: `CarStatusBadge`, `CarListTable`, `CarFormDrawer`, `CarIssueDialog`, `CarRespondForm`, `CarVerifyForm`, `CarTimeline`, `CarAttachmentUpload`, `CarMrSignDialog`, `CarDetailClient`, `CarFormDrawerTrigger`; pages: `/qms/car`, `/qms/car/[id]`, `/(user)/car`, `/(user)/car/[id]`, `/approve/car/[id]/mr`; sidebar links added).
- [x] Add MS Graph group search â€” `searchEntraGroups` in `ms-graph.ts`, new `GET /api/ms-graph/groups/search` route.
- [x] Implement `GraphUserPicker` and `GraphGroupPicker` reusable components in `components/shared/`. Used in `CarFormDrawer` for issuer (auto-fills position from MS365 job title) and email group selection.
- [x] Added practical concern routing to `AGENTS.md` for API/UI rule selection and audited KPI module against the updated rules.
- [x] Audited CAR module against the updated API and UI rules; identified service-layer Prisma usage, enterprise table gaps, and MR sign UI standard gaps.
- [x] Rebaselined `plan/QMS Plan.csv` into an Agile SDLC backlog using the actual repository state for CAR, announcements, document control, audit modules, and next-month AI RAG work.
- [x] Implemented CAR hardening from audit: paginated/searchable/filterable `/api/car`, enterprise CAR list UI with mobile cards, repository-owned CAR write paths, and Vitest coverage for CAR service list/issue flows.
- [x] Materialized `portable/` in-repo and moved generic reusable rules into `portable/rules/*.template.md` with a reusable `portable/AGENTS.template.md` entrypoint.
- [x] Added `QMS-AUTH-CENTER-MIGRATION-PLAN.md` with a phased migration plan covering auth ownership, role cutover, local user compatibility, and SharePoint/app-only integration preservation.
- [x] Completed round-1 auth discovery and added `QMS-AUTH-CENTER-DISCOVERY.md`, covering auth entrypoints, role/department dependencies, local role mutation paths, profile authority, and Graph/SharePoint/email integration boundaries.
- [x] Rewrote `QMS-AUTH-CENTER-MIGRATION-PLAN.md` from the discovery findings into an implementation-oriented execution plan grouped by workstream, phase, and real QMS file clusters.
- [x] Added `QMS-AUTH-CENTER-CHECKLIST.md`, a step-by-step beginner-friendly checklist that breaks the migration into concrete phases, file groups, validation points, and done criteria.
- [x] Rewrote `QMS-AUTH-CENTER-CHECKLIST.md` into a near-no-guesswork beginner execution guide with per-phase goals, files to open, exact actions, verification steps, and done conditions.
- [x] Added `QMS-ROLE-RENAME-PLAN.md` as a separate post-migration refactor plan for renaming QMS roles from `USER/QMS/MR/IT` to `QMS_USER/QMS_QMS/QMS_MR/QMS_IT` with a compatibility-release strategy.
- [x] Audited real QMS code for role-rename readiness and added `migrate/QMS-ROLE-RENAME-READINESS.md`, concluding the app is not ready for a direct `QMS_*` cutover and needs a compatibility-first transition.
- [x] Implemented the first compatibility-first role-rename layer in QMS so shared auth/session/middleware/guard paths accept both legacy roles and renamed `QMS_*` roles while normalizing runtime behavior safely.
- [x] Audited what it would take to remove QMS local user/department mapping entirely and added `migrate/QMS-LOCAL-MAPPING-REMOVAL-PLAN.md`, documenting the required identity contract, schema expansion, backfill, and dual-read cutover strategy.
- [x] Designed an Auth Center session-registry API contract and updated QMS Auth Center callback to register successful app sessions back to Auth Center as a best-effort post-login step.
- [x] Implemented Phase 3 backfill and identity CRUD hardening: (1) blocked local DB role writes in `PATCH /api/it/users/[id]/role` legacy path — Auth Center is sole role authority; (2) migrated `ItUsersPage` to fetch user list directly from Auth Center API (via `listAuthCenterUsers` + `listAuthCenterRoleGrants`) in `auth_center` mode, eliminating the local DB-only path; (3) refactored `ItUserTable` to accept `AuthCenterUserRow | UserWithDept` union, routing `PATCH` calls with explicit `authUserId` + `localUserId` in auth_center mode; (4) ran `migrate/backfill-auth-user-id.ts` live — 17 rows updated across DarMaster, DarApproval, ApprovalSignature, audit_logs, kpis; (5) validated 100% coverage on all 18 business tables — ready for Phase 4 dual-write. Note: `User.authUserId` backfill requires `AUTH_CENTER_CLIENT_ID`/`AUTH_CENTER_CLIENT_SECRET` in `.env` — blocked until M2M credentials are available; 18 local users with no `employeeId` remain unmatched (all USER role, no active business records).
- [x] Dropped the dev-only `migrate/` transition/backfill artifacts and reset the `dev-qms-system` database from `prisma/schema.prisma` with `prisma db push --force-reset`, establishing a clean development baseline without legacy migration baggage.
- [x] Reworked QMS Auth Center integration to prefer delegated bearer-token forwarding over local admin-password login: stored raw Auth Center access token in the QMS session cookie, forwarded user bearer tokens from IT/profile/department flows, tightened IT admin route guards, and kept fallback admin-login support only as compatibility for non-user-driven calls.
- [x] Fixed post-Auth-Center identity mapping regressions across profile/KPI/admin flows: profile pages now prefer Auth Center profile + `authDepartmentId`; KPI pages and DAR/CAR user pages now prefer `authDepartmentId` over local department UUIDs; `/api/ms-graph/users/search` now mirrors searched Entra users into local QMS users so reviewer/approver pickers can resolve local workflow IDs; approval-config now loads Auth Center users and auto-creates missing local mirrors; IT user/department updates now resolve Auth Center department codes back to local mirrors safely; IT user UI now surfaces position data again. Verified with `npx tsc --noEmit`.
